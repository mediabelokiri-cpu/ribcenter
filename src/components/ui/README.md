# UI Components (`src/components/ui`)

This directory is reserved for reusable primitive UI components (e.g., buttons, inputs, modals, cards, badges).

### Principles
- Reusable, accessible, and uncoupled from specific domain logic.
- Implemented in future design & frontend phases.
